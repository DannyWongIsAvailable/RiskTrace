/**
 * Callback integration has been removed.
 *
 * This inert module is intentionally kept so the modified-file package can be extracted directly
 * over an older checkout without leaving the previous callback implementation behind.
 */
export {}
